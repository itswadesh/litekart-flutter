import 'package:flutter/rendering.dart';

class Radii {
  static const BorderRadiusGeometry k4pxRadius =
      BorderRadius.all(Radius.circular(4));
  static const BorderRadiusGeometry k5pxRadius =
      BorderRadius.all(Radius.circular(5));
  static const BorderRadiusGeometry k8pxRadius =
      BorderRadius.all(Radius.circular(8));
  static const BorderRadiusGeometry k10pxRadius =
      BorderRadius.all(Radius.circular(10));
  static const BorderRadiusGeometry k15pxRadius =
      BorderRadius.all(Radius.circular(15));
  static const BorderRadiusGeometry k16pxRadius =
      BorderRadius.all(Radius.circular(16));
  static const BorderRadiusGeometry k20pxRadius =
      BorderRadius.all(Radius.circular(20));
  static const BorderRadiusGeometry k24pxRadius =
      BorderRadius.all(Radius.circular(24));
}
